# HREFv3 PROB Specific Configuration Options

[config]
MODEL = CONUSHREFX_PROB

LEAD_SEQ_MIN = 1
LEAD_SEQ_MAX = 48

[dir]
MODEL_INPUT_DIR = /gpfs/hps2/ptmp/Matthew.Pyle/com/hiresw/test/ 

[filename_templates]
#MODEL_TEMPLATE = href.{init?fmt=%Y%m%d}_expv3/ensprod/href.t{init?fmt=%2H}z.conus.prob.f{lead?fmt=%2H}.grib2

MODEL_TEMPLATE = hrefx.{init?fmt=%Y%m%d}.t{init?fmt=%2H}z.conus.nmep.f{lead?fmt=%2H}.{ENV[ACCUM_BEG]}-{ENV[ACCUM_END]}
